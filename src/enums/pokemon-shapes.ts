/**
 * This enum is used to classify Pokemon species by their body shape/form.
 * The mainline games use symbols to represent the different shapes, so all labels are fan-created.
 * These labels are from PokeAPI.
 */

export enum PokemonShapes {
  BALL = 1,
  SQUIGGLE,
  FISH,
  ARMS,
  BLOB,
  UPRIGHT,
  LEGS,
  WINGS,
  QUADRUPED,
  TENTACLES,
  HEADS,
  HUMANOID,
  BUG_WINGS,
  ARMOR,
}
